const { activation_engine } = require("../Helpers/product_rule_based_acti_engine.js");

class helper {
  activation_engine(args) {
    activation_engine(args);
  }
}

const Helper = () => {
    return new helper();
}

exports.Helper = Helper;