
const { transaction_engine } = require("./transaction_engine");

class helper {
  
  transaction_engine(args) {
    transaction_engine(args);
  }
}

const Helper = () => {
  return new helper();
};

exports.Helper = Helper;
